<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--음성인식-->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
    </queries>

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App"
        tools:targetApi="31">
        <activity
            android:name=".presentation.login.LandingActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".presentation.login.LoginActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".presentation.login.SignupActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".presentation.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".presentation.home.game.GameTabActivity"
            android:exported="false">
        </activity>

        <activity android:name=".presentation.home.ai_practice.AIPracticeActivity"
            android:exported="false">
        </activity>

        <activity android:name=".presentation.home.ai_practice.previous_practice.PreviousPracticeActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".presentation.community.profile_detail.ImagePopupActivity"
            android:exported="false">
        </activity>
    </application>

</manifest>